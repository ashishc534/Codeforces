#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, t;
    cin >> n;
    while(n--)
    {
    	cin>>t;
        if (t == 1)
        {
            cout << -1 << endl;
            return 0;
        }
    }
    cout << 1 << endl;
    return 0;
}
